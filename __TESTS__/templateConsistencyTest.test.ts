test('Critical files are where they should be', () => {
  // TODO: ADD TEST
});

test('All templates share a similar directory structure', () => {
  // TODO: ADD TEST
});

test('Package versions are the same across all templates', () => {
  // TODO: ADD TEST
});
